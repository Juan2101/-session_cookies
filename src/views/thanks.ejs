<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="stylesheets/style.css">
    <title>session</title>
</head>
<% if (locals.user) { %>
    <style>
      :root{
        --color: <%= user.background %>
      }
    
      body{
        background-color: var(--color);
      }
    </style>
  <% } %>
<body>
    <div class="conteiner">
        <h2><%= user.name %>, Gracias por visitarnos</h2>
        <form class="olvidar" action="/destroy" method="GET">
            <button type="submit">Olvidar color</button>
        </form>
    </div>
</body>
</html>